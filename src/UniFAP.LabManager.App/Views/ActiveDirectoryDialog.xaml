<Window x:Class="UniFAP.LabManager.App.Views.ActiveDirectoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:UniFAP.LabManager.App.ViewModels"
        xmlns:conv="clr-namespace:UniFAP.LabManager.App.Converters"
        mc:Ignorable="d"
        Title="Ingressar no Active Directory — UniFAP"
        Height="540" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource AppBgBrush}"
        d:DataContext="{d:DesignInstance vm:ActiveDirectoryDialogViewModel}">

    <Window.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Border Padding="28">
        <StackPanel>
            <TextBlock Text="INGRESSAR NO ACTIVE DIRECTORY" Style="{StaticResource TitleHeader}" FontSize="18" Foreground="{StaticResource PrimaryBrush}" />
            <TextBlock Text="Insira as credenciais de um administrador do domínio para ingressar este computador na rede institucional." Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,18" />

            <!-- PRE-VALIDAÇÃO STATUS -->
            <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Pré-Validação de Rede:" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}" />
                    <TextBlock Text="{Binding ValidationMessage}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource SuccessBrush}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <!-- DOMÍNIO E DC (SOMENTE LEITURA) -->
            <TextBlock Text="Domínio Institucional:" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
            <TextBox Text="{Binding Domain}" IsReadOnly="True" Margin="0,0,0,12" Background="#1E293B" />

            <TextBlock Text="Unidade Organizacional (OU):" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4" />
            <TextBox Text="{Binding OuPath}" IsReadOnly="True" Margin="0,0,0,14" Background="#1E293B" FontSize="11" />

            <!-- CREDENCIAIS -->
            <TextBlock Text="Usuário Administrador de Domínio:" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,4" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

            <TextBlock Text="Senha:" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,4" />
            <PasswordBox x:Name="pwdBox" PasswordChanged="PwdBox_PasswordChanged" Margin="0,0,0,24" />

            <!-- BOTÕES -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="CANCELAR" Command="{Binding CancelCommand}" Style="{StaticResource SecondaryButton}" />
                <Button Grid.Column="2" Content="CONTINUAR" Command="{Binding ConfirmCommand}" Style="{StaticResource PrimaryButton}" />
            </Grid>
        </StackPanel>
    </Border>
</Window>
