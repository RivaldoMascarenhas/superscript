<UserControl x:Class="UniFAP.LabManager.App.Views.ExecutionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:UniFAP.LabManager.App.ViewModels"
             xmlns:conv="clr-namespace:UniFAP.LabManager.App.Converters"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1100"
             d:DataContext="{d:DesignInstance vm:ExecutionViewModel}">

    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
        <conv:StepStatusToColorConverter x:Key="StatusToColor" />
        <conv:StepStatusToIconConverter x:Key="StatusToIcon" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="28">
        <StackPanel MaxWidth="1100" HorizontalAlignment="Stretch">

            <!-- CABEÇALHO DO PROCESSO -->
            <Grid Margin="0,0,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="PREPARANDO COMPUTADOR" Style="{StaticResource TitleHeader}" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding CurrentJob.ProfileDisplayName, StringFormat='Perfil: {0} '}" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" />
                        <TextBlock Text="{Binding CurrentJob.Id, StringFormat='• {0}'}" FontSize="14" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" Margin="6,0,0,0" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding ElapsedTimeString, StringFormat='Tempo decorrido: {0}'}" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" />
                </StackPanel>
            </Grid>

            <!-- BARRA DE PROGRESSO E ETAPA ATUAL -->
            <Border Style="{StaticResource CardPanel}" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="{Binding CurrentStepName}" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
                        <TextBlock Text="{Binding ProgressPercentage, StringFormat='{}{0:0}%'}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Right" />
                    </Grid>

                    <ProgressBar Value="{Binding ProgressPercentage}" Maximum="100" Style="{StaticResource ModernProgressBar}" Height="12" Margin="0,0,0,6" />
                </StackPanel>
            </Border>

            <!-- PAINEL DE SUCESSO / CONCLUSÃO (APENAS QUANDO FINALIZADO) -->
            <Border Background="#064E3B" BorderBrush="{StaticResource SuccessBrush}" BorderThickness="1" CornerRadius="10" Padding="20" Margin="0,0,0,20" Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVis}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="✓ PREPARAÇÃO CONCLUÍDA" FontSize="20" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" Margin="0,0,0,4" />
                        <TextBlock Text="Todas as etapas de padronização, softwares e configurações institucionais foram processadas." Foreground="#A7F3D0" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="📄 Ver Relatório TXT" Command="{Binding OpenReportTxtCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" />
                        <Button Content="📊 Relatório JSON" Command="{Binding OpenReportJsonCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" />
                        <Button Content="📁 Pasta de Logs" Command="{Binding OpenLogsFolderCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" />
                        <Button Content="Voltar ao Início" Command="{Binding ReturnToDashboardCommand}" Style="{StaticResource PrimaryButton}" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- GRID PRINCIPAL DE EXECUÇÃO: ETAPAS + SOFTWARES + LOGS -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="1.8*" />
                </Grid.ColumnDefinitions>

                <!-- COLUNA ESQUERDA: LISTA DE ETAPAS E SOFTWARES -->
                <StackPanel Grid.Column="0">
                    <Border Style="{StaticResource CardPanel}" Padding="16">
                        <StackPanel>
                            <TextBlock Text="ETAPAS DO SISTEMA" Style="{StaticResource CardTitle}" FontSize="13" Foreground="{StaticResource PrimaryBrush}" />

                            <ItemsControl ItemsSource="{Binding Steps}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="24" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Status, Converter={StaticResource StatusToIcon}}" Foreground="{Binding Status, Converter={StaticResource StatusToColor}}" FontWeight="Bold" FontSize="14" />
                                            <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- LISTA DE SOFTWARES NA FILA -->
                    <Border Style="{StaticResource CardPanel}" Padding="16">
                        <StackPanel>
                            <TextBlock Text="FILA DE SOFTWARES" Style="{StaticResource CardTitle}" FontSize="13" Foreground="{StaticResource PrimaryBrush}" />

                            <ScrollViewer MaxHeight="220" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding SoftwareQueue}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="24" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0" Text="{Binding Status, Converter={StaticResource StatusToIcon}}" Foreground="{Binding Status, Converter={StaticResource StatusToColor}}" FontWeight="Bold" FontSize="13" />
                                                <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                                                <TextBlock Grid.Column="2" Text="{Binding Status}" FontSize="11" Foreground="{Binding Status, Converter={StaticResource StatusToColor}}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- COLUNA DIREITA: OPÇÕES AUTOMÁTICAS E LIVE CONSOLE -->
                <StackPanel Grid.Column="2">
                    <Border Style="{StaticResource CardPanel}" Padding="16" Margin="0,0,0,14">
                        <StackPanel>
                            <TextBlock Text="POLÍTICAS DE EXECUÇÃO AUTOMÁTICA" Style="{StaticResource CardTitle}" FontSize="13" Foreground="{StaticResource PrimaryBrush}" />

                            <WrapPanel Orientation="Horizontal">
                                <CheckBox IsChecked="True" Content="Continuar automaticamente" IsEnabled="False" Margin="0,0,16,6" />
                                <CheckBox IsChecked="True" Content="Instalar silenciosamente" IsEnabled="False" Margin="0,0,16,6" />
                                <CheckBox IsChecked="{Binding CurrentJob.AutoReboot}" Content="Reiniciar quando necessário" IsEnabled="False" Margin="0,0,16,6" />
                                <CheckBox IsChecked="{Binding CurrentJob.AutoResume}" Content="Retomar após reinicialização" IsEnabled="False" Margin="0,0,16,6" />
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <!-- LIVE CONSOLE LOGS -->
                    <Border Style="{StaticResource CardPanel}" Padding="14">
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="REGISTRO EM TEMPO REAL" Style="{StaticResource CardTitle}" FontSize="13" Foreground="{StaticResource PrimaryBrush}" />
                                <Button Content="Logs Detalhados" Command="{Binding ToggleAdvancedLogsCommand}" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Right" Padding="10,4" FontSize="11" />
                            </Grid>

                            <!-- JANELA DE CONSOLE -->
                            <Border Background="#020617" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6" Padding="12" Height="340">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding LiveLogsList}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" FontFamily="{StaticResource CodeFont}" FontSize="12" Foreground="#A7F3D0" Margin="0,1" />
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Border>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

        </StackPanel>
    </ScrollViewer>
</UserControl>
