<UserControl x:Class="UniFAP.LabManager.App.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:UniFAP.LabManager.App.ViewModels"
             xmlns:conv="clr-namespace:UniFAP.LabManager.App.Converters"
             mc:Ignorable="d" d:DesignHeight="750" d:DesignWidth="1100"
             d:DataContext="{d:DesignInstance vm:HistoryViewModel}">

    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
        <conv:StepStatusToColorConverter x:Key="StatusToColor" />
        <conv:StepStatusToIconConverter x:Key="StatusToIcon" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="28">
        <StackPanel MaxWidth="1100" HorizontalAlignment="Stretch">

            <!-- CABEÇALHO -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Histórico de Preparações" Style="{StaticResource TitleHeader}" />
                    <TextBlock Text="Registro persistente de todas as execuções realizadas com detalhes e relatórios." Style="{StaticResource SubtitleHeader}" />
                </StackPanel>
                <Button Grid.Column="1" Content="↻ Atualizar Lista" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryButton}" VerticalAlignment="Center" />
            </Grid>

            <!-- GRID PRINCIPAL: LISTA + DETALHES -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="1.8*" />
                </Grid.ColumnDefinitions>

                <!-- LISTA DE JOBS ANTERIORES -->
                <Border Grid.Column="0" Style="{StaticResource CardPanel}" Padding="14">
                    <ScrollViewer MaxHeight="540" VerticalScrollBarVisibility="Auto">
                        <ListBox ItemsSource="{Binding JobsHistory}" SelectedItem="{Binding SelectedJob, Mode=TwoWay}" Background="Transparent" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8" Cursor="Hand">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding Id}" FontWeight="Bold" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" />
                                                <TextBlock Text="{Binding ProfileDisplayName}" FontSize="12" Foreground="{StaticResource PrimaryBrush}" />
                                                <TextBlock Text="{Binding CreatedAt, StringFormat='Data: {0:dd/MM/yyyy HH:mm}'}" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding Status}" FontSize="12" FontWeight="SemiBold" Foreground="{Binding Status, Converter={StaticResource StatusToColor}}" VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </ScrollViewer>
                </Border>

                <!-- DETALHES DO JOB SELECIONADO -->
                <Border Grid.Column="2" Style="{StaticResource CardPanel}" Padding="20">
                    <StackPanel>
                        <TextBlock Text="DETALHES DA EXECUÇÃO" Style="{StaticResource CardTitle}" Foreground="{StaticResource PrimaryBrush}" />

                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Job ID:" Foreground="{StaticResource TextMutedBrush}" Margin="0,3" />
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedJob.Id}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,3" />

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Computador:" Foreground="{StaticResource TextMutedBrush}" Margin="0,3" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedJob.TargetComputerName}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,3" />

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Perfil:" Foreground="{StaticResource TextMutedBrush}" Margin="0,3" />
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedJob.ProfileDisplayName}" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" Margin="0,3" />

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Status:" Foreground="{StaticResource TextMutedBrush}" Margin="0,3" />
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedJob.Status}" FontWeight="Bold" Foreground="{Binding SelectedJob.Status, Converter={StaticResource StatusToColor}}" Margin="0,3" />
                        </Grid>

                        <Separator Background="{StaticResource BorderBrush}" Margin="0,6,0,14" />

                        <!-- ETAPAS DO JOB SELECIONADO -->
                        <TextBlock Text="Etapas Realizadas:" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8" />
                        <ScrollViewer MaxHeight="220" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16">
                            <ItemsControl ItemsSource="{Binding SelectedJob.Steps}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,3">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="20" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Status, Converter={StaticResource StatusToIcon}}" Foreground="{Binding Status, Converter={StaticResource StatusToColor}}" FontWeight="Bold" />
                                            <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                                            <TextBlock Grid.Column="2" Text="{Binding Status}" FontSize="11" Foreground="{Binding Status, Converter={StaticResource StatusToColor}}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <!-- BOTÕES DE RELATÓRIO -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="📄 Abrir Relatório TXT" Command="{Binding OpenReportTxtCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" />
                            <Button Content="📊 Relatório JSON" Command="{Binding OpenReportJsonCommand}" Style="{StaticResource PrimaryButton}" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

        </StackPanel>
    </ScrollViewer>
</UserControl>
