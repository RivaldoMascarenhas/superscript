<UserControl x:Class="UniFAP.LabManager.App.Views.PreparationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:UniFAP.LabManager.App.ViewModels"
             xmlns:conv="clr-namespace:UniFAP.LabManager.App.Converters"
             mc:Ignorable="d" d:DesignHeight="850" d:DesignWidth="1100"
             d:DataContext="{d:DesignInstance vm:PreparationViewModel}">

    <UserControl.Resources>
        <conv:BooleanToVisibilityConverter x:Key="BoolToVis" />
        <conv:StepStatusToColorConverter x:Key="StatusToColor" />
        <conv:StepStatusToIconConverter x:Key="StatusToIcon" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="28">
        <StackPanel MaxWidth="1100" HorizontalAlignment="Stretch">

            <!-- CABEÇALHO -->
            <TextBlock Text="Preparar Computador" Style="{StaticResource TitleHeader}" />
            <TextBlock Text="Selecione o tipo de computador e o perfil desejado para iniciar a preparação automática." Style="{StaticResource SubtitleHeader}" />

            <!-- SELEÇÃO PRINCIPAL: ADMINISTRATIVO VS LABORATÓRIO -->
            <TextBlock Text="O QUE DESEJA PREPARAR?" FontSize="13" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,10" />

            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- CARD ADMINISTRATIVO -->
                <Border Grid.Column="0" Padding="20" Cursor="Hand">
                    <Border.InputBindings>
                        <MouseBinding Gesture="LeftClick" Command="{Binding SelectAdministrativeCommand}" />
                    </Border.InputBindings>
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource CardPanel}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsAdministrativeMode}" Value="True">
                                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                                    <Setter Property="BorderThickness" Value="2" />
                                    <Setter Property="Background" Value="#1E3A8A" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="💼 " FontSize="20" />
                            <TextBlock Text="ADMINISTRATIVO" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Text="Configuração institucional completa para funcionários, setores administrativos, coordenações e professores." Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" LineHeight="20" Margin="0,0,0,12" />
                        <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left">
                            <TextBlock Text="✓ Inclui Ingresso no Active Directory" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource SuccessBrush}" />
                        </Border>
                    </StackPanel>
                </Border>

                <!-- CARD LABORATÓRIO -->
                <Border Grid.Column="2" Padding="20" Cursor="Hand">
                    <Border.InputBindings>
                        <MouseBinding Gesture="LeftClick" Command="{Binding SelectLaboratoryCommand}" />
                    </Border.InputBindings>
                    <Border.Style>
                        <Style TargetType="Border" BasedOn="{StaticResource CardPanel}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLaboratoryMode}" Value="True">
                                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                                    <Setter Property="BorderThickness" Value="2" />
                                    <Setter Property="Background" Value="#1E3A8A" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="🎓 " FontSize="20" />
                            <TextBlock Text="LABORATÓRIO" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Text="Configuração institucional para computadores acadêmicos com perfis de cursos (ADS, Engenharia, Psicologia, etc.)." Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" LineHeight="20" Margin="0,0,0,12" />
                        <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left">
                            <TextBlock Text="○ Sem AD por padrão (Isolado e Otimizado)" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMutedBrush}" />
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- CONTEÚDO PARA MODO ADMINISTRATIVO -->
            <StackPanel Visibility="{Binding IsAdministrativeMode, Converter={StaticResource BoolToVis}}">
                <Border Style="{StaticResource CardPanel}" Padding="22">
                    <StackPanel>
                        <TextBlock Text="RESUMO DO PERFIL ADMINISTRATIVO" Style="{StaticResource CardTitle}" Foreground="{StaticResource PrimaryBrush}" />
                        <TextBlock Text="O assistente executará as seguintes ações de padronização:" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,16" />

                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="✓ Padrão Windows 11 UniFAP" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4" />
                                <TextBlock Text="✓ Identidade Visual &amp; Papel de Parede" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4" />
                                <TextBlock Text="✓ Otimizações de Desempenho Seguras" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4" />
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="✓ Provisionamento de Usuários (suporte/aluno)" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4" />
                                <TextBlock Text="✓ Softwares Básicos (Chrome, Firefox, Office 365, WinRAR)" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4" />
                                <TextBlock Text="✓ Ingresso no Active Directory (OU Administrativa)" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,4" />
                            </StackPanel>
                        </Grid>

                        <Border Background="#450A0A" BorderBrush="{StaticResource DangerBrush}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,16">
                            <TextBlock Text="Programas específicos de laboratório acadêmico: NÃO SERÃO INSTALADOS." FontWeight="SemiBold" Foreground="#FCA5A5" />
                        </Border>

                        <CheckBox IsChecked="{Binding IsAdministrativeJoinAdChecked, Mode=TwoWay}" Content="Ingressar no Active Directory institucional (intranet.unifapce.edu.br)" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,4" />
                        <TextBlock Text="O assistente solicitará apenas suas credenciais de administrador de domínio e reiniciará a máquina automaticamente ao concluir." FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="24,0,0,6" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- CONTEÚDO PARA MODO LABORATÓRIO (ESCOLHA DO PERFIL) -->
            <StackPanel Visibility="{Binding IsLaboratoryMode, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="SELECIONE O PERFIL DO LABORATÓRIO:" FontSize="13" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,10" />

                <!-- LISTA DE CARDS DE PERFIS -->
                <ItemsControl ItemsSource="{Binding LaboratoryProfiles}" Margin="0,0,0,16">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="330" Style="{StaticResource CardPanelClickable}" Margin="0,0,14,14" Padding="16" Cursor="Hand">
                                <Border.InputBindings>
                                    <MouseBinding Gesture="LeftClick" Command="{Binding DataContext.SelectProfileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Id}" />
                                </Border.InputBindings>
                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayName}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,4" />
                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Height="40" Margin="0,0,0,10" />
                                    <TextBlock Text="{Binding Software.Count, StringFormat='{}{0} Softwares incluídos'}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- MODO PERSONALIZADO: SELEÇÃO LIVRE DE SOFTWARES -->
                <Border Style="{StaticResource CardPanel}" Visibility="{Binding IsCustomSoftwareMode, Converter={StaticResource BoolToVis}}" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="SELEÇÃO PERSONALIZADA DE SOFTWARES" Style="{StaticResource CardTitle}" />
                        <TextBlock Text="Marque os softwares que deseja incluir na imagem deste laboratório:" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,14" />

                        <!-- BARRA DE BUSCA E CATEGORIAS -->
                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="1.5*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBox Grid.Column="0" Text="{Binding SoftwareSearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0" ToolTip="Buscar software por nome..." />
                            <ComboBox Grid.Column="1" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" Margin="0,0,10,0" />

                            <StackPanel Grid.Column="2" Orientation="Horizontal">
                                <Button Content="Todos" Command="{Binding SelectAllSoftwareCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,6,0" Padding="12,6" />
                                <Button Content="Limpar" Command="{Binding ClearSoftwareSelectionCommand}" Style="{StaticResource SecondaryButton}" Padding="12,6" />
                            </StackPanel>
                        </Grid>

                        <!-- LISTA DE CHECKBOXES DE SOFTWARES -->
                        <ScrollViewer MaxHeight="280" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding FilteredSoftwareItems}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="240" Background="{StaticResource SurfaceLightBrush}" CornerRadius="6" Padding="10,6" Margin="0,0,8,8">
                                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center">
                                                <StackPanel Margin="6,0,0,0">
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" />
                                                    <TextBlock Text="{Binding Category}" FontSize="11" Foreground="{StaticResource TextMutedBrush}" />
                                                </StackPanel>
                                            </CheckBox>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <TextBlock Text="{Binding SelectedSoftwareCount, StringFormat='{}{0} programas selecionados para instalação'}" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,12,0,0" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- CARD OBRIGATÓRIO: USUÁRIO SUPORTE -->
            <Border Style="{StaticResource CardPanel}" Margin="0,0,0,20">
                <StackPanel>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="👤 CONFIGURAÇÃO OBRIGATÓRIA DO ADMINISTRADOR LOCAL ('suporte')" Style="{StaticResource CardTitle}" FontSize="14" Foreground="{StaticResource PrimaryBrush}" />
                        <Border Background="{StaticResource SurfaceLightBrush}" CornerRadius="4" Padding="8,2" HorizontalAlignment="Right">
                            <TextBlock Text="Obrigatório em Todas as Máquinas" FontSize="11" FontWeight="Bold" Foreground="{StaticResource WarningBrush}" />
                        </Border>
                    </Grid>
                    <TextBlock Text="A conta local 'suporte' com privilégios de Administrador é configurada em todas as estações (Administrativo e Laboratório) para manutenção da equipe de TI. Defina e confirme a senha abaixo:" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,14" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Senha do Administrador 'suporte' *:" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,4" />
                            <TextBox Text="{Binding SupportAdminPassword, UpdateSourceTrigger=PropertyChanged}" ToolTip="Digite a senha de administrador local..." />
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Confirmar Senha do Administrador 'suporte' *:" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,4" />
                            <TextBox Text="{Binding SupportAdminPasswordConfirm, UpdateSourceTrigger=PropertyChanged}" ToolTip="Confirme a mesma senha digitada..." />
                        </StackPanel>
                    </Grid>

                    <!-- Mensagem de Validação -->
                    <TextBlock Text="{Binding ValidationErrorMessage}" Foreground="{StaticResource DangerBrush}" FontWeight="Bold" FontSize="12" Margin="0,10,0,0" Visibility="{Binding ValidationErrorMessage, Converter={StaticResource BoolToVis}}" />
                </StackPanel>
            </Border>

            <!-- CARD DE PRE-CHECK (VALIDAÇÃO PRÉVIA) -->
            <Border Style="{StaticResource CardPanel}" Margin="0,0,0,24">
                <StackPanel>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Text="PRÉ-VALIDAÇÃO DO SISTEMA (PRE-CHECK)" Style="{StaticResource CardTitle}" FontSize="14" />
                        <Button Content="↻ Revalidar" Command="{Binding RunPreCheckCommand}" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Right" Padding="12,4" FontSize="12" />
                    </Grid>

                    <ItemsControl ItemsSource="{Binding PreCheckReport.Items}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="24" />
                                        <ColumnDefinition Width="220" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding Status, Converter={StaticResource StatusToIcon}}" Foreground="{Binding Status, Converter={StaticResource StatusToColor}}" FontWeight="Bold" FontSize="14" />
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" />
                                    <TextBlock Grid.Column="2" Text="{Binding Message}" Foreground="{StaticResource TextSecondaryBrush}" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Separator Background="{StaticResource BorderBrush}" Margin="0,12" />
                    <TextBlock Text="{Binding PreCheckReport.Summary}" FontWeight="Bold" FontSize="14" Foreground="{StaticResource PrimaryBrush}" />
                </StackPanel>
            </Border>

            <!-- BOTÕES DE AÇÃO: SIMULAR / EXECUTAR -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,30">
                <Button Content="🔍 Simular (Dry Run)" Command="{Binding StartDryRunCommand}" Style="{StaticResource SecondaryButton}" Margin="0,0,14,0" FontSize="15" Padding="20,12" />
                <Button Content="⚡ PREPARAR COMPUTADOR" Command="{Binding StartPreparationCommand}" Style="{StaticResource PrimaryButton}" FontSize="15" Padding="28,12" />
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
